# mgvs

LLM-guided structured search over a canonical reasoning state with deterministic multi-level verification.

## Status

Bootstrap skeleton for a production-style research/competition codebase.

## Quickstart

```bash
python -m pip install -e .
mgvs
```
